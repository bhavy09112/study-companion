[project]
name = "study-companion"
version = "1.0.0"
description = "Local-first, RAG-augmented study assistant with fine-tuning pipeline"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py310"
